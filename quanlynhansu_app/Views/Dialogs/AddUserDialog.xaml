<Window x:Class="quanlynhansu_app.Views.Dialogs.AddUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Thêm tài khoản mới" Height="450" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="➕ Thêm tài khoản" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBox x:Name="txtUsername" materialDesign:HintAssist.Hint="Tên đăng nhập *" 
                     Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15"/>

            <PasswordBox x:Name="txtPassword" materialDesign:HintAssist.Hint="Mật khẩu *" 
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}" Margin="0,0,0,15" Height="45"/>

            <TextBox x:Name="txtEmail" materialDesign:HintAssist.Hint="Email" 
                     Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15"/>

            <TextBlock Text="Vai trò" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="cboRole" SelectedIndex="0" Margin="0,0,0,20" 
                      Style="{StaticResource MaterialDesignOutlinedComboBox}">
                <ComboBoxItem Content="user" Tag="user"/>
                <ComboBoxItem Content="admin" Tag="admin"/>
            </ComboBox>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Hủy" Style="{StaticResource MaterialDesignOutlinedButton}" 
                        Click="BtnCancel_Click" Margin="0,0,10,0"/>
                <Button Content="Lưu" Style="{StaticResource PrimaryButtonStyle}" 
                        Click="BtnSave_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>